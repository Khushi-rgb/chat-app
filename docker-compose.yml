services:

  client:
    build: ./client
    ports:
      - "5173:5173"
    depends_on:
      - server

  server:
    build: ./server
    ports:
      - "5000:5000"
    depends_on:
      - redis

  redis:
    image: redis
    ports:
      - "6379:6379"